# ============================================================
#  Paste your Gemini API key below — this file stays on the
#  server and is NEVER sent to the browser or committed to Git
# ============================================================
GEMINI_API_KEY=YOUR_API_KEY_HERE

# Optional: change the port the server listens on
PORT=3000
